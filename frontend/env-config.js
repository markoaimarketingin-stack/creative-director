// Runtime config for browser deployments (e.g. Vercel static hosting).
// Set BACKEND_URL to your deployed API origin.
window.__APP_CONFIG__ = window.__APP_CONFIG__ || {
  BACKEND_URL: "http://127.0.0.1:8000"
};
